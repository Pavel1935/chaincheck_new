name: Smoke tests  # Имя workflow (видно в Actions)

on:
  push:
    branches: [ main ]           # Запуск при пушах в main
  pull_request:
  schedule:
    - cron: "0 5 * * *"          # Автозапуск по расписанию: 05:00 UTC (08:00 МСК)

jobs:
  smoke:
    runs-on: ubuntu-latest
    container:
      image: mcr.microsoft.com/playwright/python:v1.56.0-jammy
      options: --ipc=host --shm-size=1g

    permissions:
      contents: write
      pages: write
      id-token: write

    steps:
      # 1️⃣ Забираем код
      - name: Checkout repository
        uses: actions/checkout@v4

      # 2️⃣ Ставим зависимости (pytest + allure)
      - name: Install dependencies
        run: |
          pip install -r requirements.txt
          pip install "pytest<8.3.0" "allure-pytest>=2.13.2"

      # 3️⃣ Ставим Allure CLI для генерации HTML
      - name: Install Allure CLI
        run: |
          apt-get update
          apt-get install -y default-jre wget unzip
          ALLURE_VERSION=2.29.0
          wget -q https://github.com/allure-framework/allure2/releases/download/${ALLURE_VERSION}/allure-${ALLURE_VERSION}.zip -O /tmp/allure.zip
          unzip -q /tmp/allure.zip -d /opt/
          ln -sf /opt/allure-${ALLURE_VERSION}/bin/allure /usr/bin/allure
          allure --version

      # 4️⃣ Чистим папку allure-results
      - name: Prepare allure-results
        run: rm -rf allure-results && mkdir -p allure-results

      # 5️⃣ Добавляем метаданные
      - name: Add Allure metadata
        run: |
          mkdir -p allure-results
          cat > allure-results/executor.json <<'EOF'
          {
            "name": "GitHub Actions",
            "type": "github",
            "reportName": "Smoke tests",
            "buildName": "${{ github.workflow }} #${{ github.run_number }}",
            "buildUrl": "https://github.com/${{ github.repository }}/actions/runs/${{ github.run_id }}",
            "environment": "${{ github.ref_name }}"
          }
          EOF
          echo "branch=${{ github.ref_name }}" >> allure-results/environment.properties
          echo "runner=ubuntu-latest" >> allure-results/environment.properties

      # 6️⃣ Запуск API smoke тестов
      - name: Run API smoke tests
        run: |
          pytest -m "smoke and api" -vv -ra \
                 --alluredir=allure-results \
                 --clean-alluredir

      # 7️⃣ Пауза 3 минуты (капча)
      - name: Wait before UI tests
        run: sleep 180

      # 8️⃣ Запуск UI smoke тестов
      - name: Run UI smoke tests
        run: |
          pytest -m "smoke and ui" -vv -ra \
                 --alluredir=allure-results

      # 9️⃣ Проверяем наличие allure-results
      - name: Check allure-results content
        if: always()
        run: |
          echo "Checking allure-results folder:"
          ls -la allure-results || echo "❌ No allure-results directory found"

      # 🔟 Загружаем "сырые" результаты
      - name: Upload Allure results (raw)
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: allure-results
          path: allure-results
          if-no-files-found: warn

      # 1️⃣1️⃣ Генерация HTML отчёта
      - name: Generate Allure HTML report
        if: always()
        run: |
          export ALLURE_BASE_URL="/${GITHUB_REPOSITORY#*/}/"
          allure generate allure-results -o allure-report --clean
          ls -la allure-report | head -n 30

      # 1️⃣2️⃣ Публикация на GitHub Pages
      - name: Deploy Allure Report to GitHub Pages
        if: always() && github.ref == 'refs/heads/main'
        uses: peaceiris/actions-gh-pages@v3
        with:
          github_token: ${{ secrets.GITHUB_TOKEN }}
          publish_dir: ./allure-report
          publish_branch: gh-pages
          force_orphan: true

      # 1️⃣3️⃣ Комментарий в PR со ссылкой
      - name: Comment report URL
        if: always()
        uses: marocchino/sticky-pull-request-comment@v2
        with:
          header: smoke-report
          message: "✅ Allure report: https://${{ github.repository_owner }}.github.io/${{ github.event.repository.name }}/"
